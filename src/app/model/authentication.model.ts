export interface ISignIn {
    username: string;
    password: string;
}

export interface SignIn {
    username: string;
    firstName: string;
    lastName: string;
    email: string;
    group: string;
}